用户提供自己的年龄、性别、现用药、体检，以及细胞比例、对数转录组或 TCR 指标表。

技能使用期刊页面 Table S3 的冻结系数，不向用户索取 GEO、STRING、PDF、HRA 或这张表。

报告先给论文卡片。测量齐的 pAge、tAge、TCRAge 按系数计算。ptAge、immAge、bulk-immAge 不算。现用药对不上时写「不能据此停」。体检只作上下文，不改时钟，也不增删名单。
