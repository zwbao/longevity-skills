# 论文、仓库、另一套同名东西

Son 等，Nature Aging（2026），doi:10.1038/s43587-026-01078-2。EuropePMC fullTextXML 返回 HTTP 500。全文改从 NCBI PMC13004671 的 XML 读到。

| | 内容 |
| --- | --- |
| 论文声称 | 520 份血浆。可及性平均：健康 93.2%（n = 227），MCI 92.0%（n = 135），AD 91.1%（n = 158）。C1QA、CLUS 和 ApoB 三个标志在 470 份样本、18 个算法里的无监督测试准确率是 83.44%。正文没有给出这套分类器的系数。 |
| 代码实际算 | 构建清单没有方法仓库。三个肽段只照录，因为 93.2%、92.0%、91.1% 是 879 条肽的组平均，不是这三个肽的平均。若用户另交跨肽段的整体可及性，本技能算它与这三组平均的绝对差。不把 83.44% 套到这个人身上，也不套用未印出的深度学习权重。图注把 C1QA 肽段写成 GFCDTTNLKGLF，正文模型句写成 GFCDTTNKGLF。报告用模型句的拼法。 |
| 同名的另一套 | 文中 STRING 置信度 0.4 用来画 ApoE 互作，不是这三蛋白面板的分类器。技能不向用户要 STRING。 |
